const dictionary = {
    en: {
        messages: {
            required: (field, val) => `Field ${field} is required`
        }
    },
    es: {
        messages: {
            required: (field, val) => `Campo ${field} requerido`
        }
    },
};

export default dictionary;
